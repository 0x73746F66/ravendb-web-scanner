charset: utf8
table: '`scans`.`scan_log`'
field:
  id: 
    pk: True
    type: int
  domain:
    type: str
    max_len: 255
    nullable: False
  local_file:
    type: str
    max_len: 255
    nullable: False
  czdap_id:
    type: int
    default: null
    nullable: True
  nameserver:
    type: str
    max_len: 255
    nullable: False
  ttl:
    type: int
    nullable: False
  scanned:
    type: datetime
    default: UTC_TIMESTAMP
schema: >
  CREATE TABLE `scan_log` (
    `id` int(10) unsigned NOT NULL AUTO_INCREMENT,
    `domain` varchar(255) DEFAULT NULL,
    `local_file` varchar(255) DEFAULT NULL,
    `czdap_id` int(10) unsigned DEFAULT NULL,
    `nameserver` varchar(255) NOT NULL,
    `ttl` int(10) unsigned NOT NULL,
    `scanned` timestamp NULL DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`)
  ) ENGINE=InnoDB DEFAULT CHARSET=utf8;
